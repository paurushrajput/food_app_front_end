import { HashTruncateDirective } from './hash-truncate.directive';

describe('HashTruncateDirective', () => {
  it('should create an instance', () => {
    const directive = new HashTruncateDirective();
    expect(directive).toBeTruthy();
  });
});
